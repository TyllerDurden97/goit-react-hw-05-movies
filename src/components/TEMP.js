 // const genresNames = movDetails.genres.map(({ name }) => {
   //    const genres = [];
   //    genres.push(name);
   //    return genres;
   // });
   // console.log(genresNames);

   // const [ ...genres ] = movDetails.genres;

    // if (Object.keys(movDetails).length !== 0) {
   //       console.log('Не пуст')
   // }